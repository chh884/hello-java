# 从 0 到 1 入门路线：Java + SpringBoot + Redis + MQ + GitHub

## 🎯 总体规划

- **周期**：约 4–6 个月（按每天投入 2–3 小时估算）
- **学习顺序逻辑**：按依赖关系排列
  `版本控制(Git/GitHub)` → `语言核心(Java)` → `数据库+Web框架(SpringBoot)` → `缓存(Redis)` → `消息队列(MQ)` → `综合实战`
- **原则**：每一步都有明确产出物；不跳级；所有代码提交到 GitHub。

---

## 📌 阶段 0：前置准备（1–2 周）

**目标**：搭好环境，学会版本控制（后续所有阶段的地基）。

| 学习项 | 要点 | 产出物 |
|---|---|---|
| Git & GitHub | `clone/add/commit/push/pull`、分支 `branch/merge`、PR 流程、`.gitignore` | 在 GitHub 建第一个仓库，提交 README |
| JDK 17+ 安装 | 配置 `JAVA_HOME`，验证 `java -version` | 终端能输出版本 |
| IDE | 安装 IntelliJ IDEA（社区版免费即可） | 能新建 Java 项目 |
| 构建工具 | Maven 基本概念（`pom.xml`、依赖、生命周期） | 用 Maven 跑通 hello world |

**资源**：GitHub 官方 Git 教程 https://docs.github.com/zh/get-started ；廖雪峰 Git 教程。

---

## 📌 阶段 1：Java 语言核心（6–8 周）

最重要的一步，不要跳。后续 SpringBoot 的所有"魔法"都建立在这里。

1. **基础语法**（1 周）：变量、运算符、控制流、数组、方法
2. **面向对象 OOP**（2 周）：类与对象、封装/继承/多态、接口与抽象类、`static`/`final`
3. **集合框架**（1 周）：`List/Set/Map`、`ArrayList/HashMap` 原理
4. **异常处理**（3 天）：`try-catch-finally`、受检 vs 非受检异常
5. **IO 与文件**（3 天）：字节流/字符流、NIO 基础
6. **多线程并发**（1.5 周）：`Thread/Runnable`、`synchronized`、线程池、`CompletableFuture`、`volatile`
7. **Java 8+ 新特性**（1 周）：Lambda、Stream API、`Optional`、新日期 API
8. **JVM 基础（够用即可）**：内存结构、垃圾回收概念、类加载机制

**产出物**：命令行小工具（简易通讯录/记账本），集合存储 + 文件持久化。

---

## 📌 阶段 2：数据库 + SpringBoot Web（4–6 周）

从"写程序"到"写工程"的跨越。

1. **MySQL**（1.5 周）：增删改查、连表、索引概念、建表设计
2. **Spring 核心概念**（1 周）：IoC（控制反转）/ DI（依赖注入）、AOP、Bean 生命周期
3. **SpringBoot 入门**（1 周）：自动配置原理、`starter`、`application.yml`、热部署
4. **Spring MVC / REST API**（1 周）：`@RestController`、`@GetMapping/@PostMapping`、参数校验、统一异常处理、统一响应格式
5. **数据访问层**（1.5 周）：选其一
   - **MyBatis**（国内主流，推荐）：XML 映射、动态 SQL
   - 或 Spring Data JPA（更自动，国内用得少）
6. **进阶（按需）**：Spring Security 基础、Swagger/Knife4j 接口文档、日志

**产出物**：`User` 用户增删改查 REST API，Postman/Apifox 调通。

---

## 📌 阶段 3：Redis 缓存（2–3 周）

前置：已会 SpringBoot。

1. **Redis 基础**（1 周）：5 种核心数据结构（String/List/Hash/Set/ZSet）、`EXPIRE`、常用命令
2. **Spring Data Redis**（1 周）：`RedisTemplate`、`@Cacheable`、序列化配置、连接池
3. **典型场景**：缓存查询结果、缓存击穿/穿透/雪崩、分布式锁、Session 共享
4. **持久化与高可用（了解）**：RDB/AOF、主从、哨兵

**产出物**：给阶段 2 的用户查询接口加 Redis 缓存，对比前后响应耗时。

---

## 📌 阶段 4：消息队列 MQ（2–3 周）

前置：已会 SpringBoot + 一个数据库。

1. **MQ 概念**（3 天）：为什么需要 MQ？同步 vs 异步、点对点 vs 发布订阅
2. **选型**：入门先学 **RabbitMQ**（轻量、概念清晰）；进阶可看 Kafka
3. **RabbitMQ 实战**（1.5 周）：Exchange/Queue/Binding、Direct/Fanout/Topic 模式、可靠性投递、死信队列
4. **Spring AMQP**：`@RabbitListener`、生产者/消费者写法

**产出物**：用户注册后发消息到 MQ，由消费者异步发"欢迎邮件"（模拟）。

---

## 📌 阶段 5：综合项目实战（3–4 周）

把前面所有技能串起来。建议方向（选一个）：

- **简易电商订单系统**：用户 + 商品 + 下单，Redis 做库存预扣、MQ 做订单异步处理
- **博客/论坛系统**：发帖 + 评论 + 点赞，Redis 做点赞计数与排行
- **秒杀系统**：Redis 防超卖、MQ 削峰（最能体现中间件价值）

**项目要覆盖**：分层架构（controller/service/dao）、统一响应、参数校验、全局异常、日志、Swagger 文档、单元测试、GitHub 多分支协作、README、Docker 部署。

---

## 🗓️ 每周节奏

| 日期 | 内容 |
|---|---|
| 周一~周五 | 学知识点 + 跟着敲代码 |
| 周六 | 做小练习 / 产出物 |
| 周日 | 复盘 + 整理笔记 + 提交 GitHub |

**贯穿全程的习惯**：从阶段 0 起，所有学习代码提交到 GitHub，养成"写代码 = 提交版本"的肌肉记忆。

---

## 📚 推荐资源（精选）

- **Java**：《Java 核心技术》、菜鸟教程、廖雪峰 Java 教程
- **SpringBoot**：官方文档、Spring Guides https://spring.io/guides 、baeldung.com
- **MySQL**：《MySQL 必知必会》
- **Redis**：《Redis 设计与实现》、Redis 官方文档
- **RabbitMQ**：官方教程（有中文版）
- **GitHub**：https://learn.github.com/

---

## ✅ 检查清单（每个阶段结束自测）

- [ ] 阶段0：能独立 `git init`、提交、推送、开 PR
- [ ] 阶段1：能独立写一个 200 行以内的 Java 命令行程序
- [ ] 阶段2：能独立从 0 搭一个带数据库的 SpringBoot CRUD 服务
- [ ] 阶段3：能讲清"缓存穿透/击穿/雪崩"及解决方案
- [ ] 阶段4：能讲清 MQ 解耦/削峰/异步三种作用
- [ ] 阶段5：GitHub 上有一个 README 完整、可运行的综合项目
