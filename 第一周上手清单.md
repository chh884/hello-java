# 第 1 周逐日上手清单（阶段 0：环境搭建）

> ⚠️ 说明：我没有办法自动检测你电脑上的工具，所以下面每一步都带了**验证命令**。
> 请在**你自己的终端**（Win 键搜 "PowerShell" 或 "cmd"）里敲这些命令。
> 验证命令如果输出版本号 = 装好了；如果提示 "不是内部或外部命令" = 没装好，重装。

---

## Day 1：装 JDK 17（Java 的运行环境）

1. 打开浏览器，下载 **Eclipse Temurin JDK 17（LTS）**：https://adoptium.net/temurin/releases/?version=17
   - 选 Windows x64，`.msi` 安装包
   - 国内访问慢可用镜像：清华源、阿里云等
2. 双击 `.msi` 安装，安装时**勾选** "Set JAVA_HOME variable" 和 "Add to PATH"
3. **验证**（开一个新终端，敲）：
   ```powershell
   java -version
   javac -version
   ```
   - 期望：两行都输出 `17.x.x` 版本号

---

## Day 2：装 Git（版本控制工具）

1. 下载 Git for Windows：https://git-scm.com/download/win
2. 安装时一路默认即可（勾选 "Git Bash Here" 方便）
3. 配置你的身份（每次提交会用到）：
   ```powershell
   git config --global user.name "你的名字"
   git config --global user.email "你的邮箱@example.com"
   ```
4. **验证**：
   ```powershell
   git --version
   git config --global user.name
   ```
   - 期望：`git version 2.x.x`，第二行输出你设的名字

---

## Day 3：注册 GitHub 并建第一个仓库

1. 注册 GitHub 账号：https://github.com
2. 右上角 **+ → New repository**：
   - 名字：`hello-java`
   - 勾选 "Add a README file"
   - Create
3. 在本地找一个文件夹，把仓库克隆下来：
   ```powershell
   cd C:\Users\Administrator
   git clone https://github.com/你的用户名/hello-java.git
   cd hello-java
   ```
4. 编辑 `README.md`，加一句话，然后提交推送：
   ```powershell
   git add .
   git commit -m "update readme"
   git push
   ```
   - 第一次推送会让你登录 GitHub，按提示在浏览器授权即可
5. **验证**：刷新 GitHub 网页，能看到你改的 README

---

## Day 4：装 IntelliJ IDEA（写 Java 的 IDE）

1. 下载 **IDEA Community（社区版，免费）**：https://www.jetbrains.com/idea/download/
   - 一定要选 "Community Edition"，不是 Ultimate
2. 安装时勾选：创建桌面快捷方式、关联 .java
3. 打开 IDEA → New Project → 选 JDK 17（刚才装的）
4. **验证**：能新建一个项目，里面写一个 `Hello.java`：
   ```java
   public class Hello {
       public static void main(String[] args) {
           System.out.println("Hello, Java!");
       }
   }
   ```
   右键 Run，看到控制台输出 `Hello, Java!` = 成功

---

## Day 5：装 Maven（Java 项目的依赖管理工具）

1. 下载 Maven：https://maven.apache.org/download.cgi → `apache-maven-3.x.x-bin.zip`
2. 解压到一个固定目录，例如 `C:\maven\apache-maven-3.9.6`
3. 配置环境变量（Win 键搜 "环境变量" → 编辑系统环境变量）：
   - 新建系统变量：`MAVEN_HOME` = `C:\maven\apache-maven-3.9.6`
   - 编辑 `Path`，加一行：`%MAVEN_HOME%\bin`
4. **关键：配置国内镜像**（否则下载依赖会非常慢）。编辑 `C:\maven\apache-maven-3.9.6\conf\settings.xml`，在 `<mirrors>` 标签内加：
   ```xml
   <mirror>
       <id>aliyunmaven</id>
       <mirrorOf>*</mirrorOf>
       <name>阿里云公共仓库</name>
       <url>https://maven.aliyun.com/repository/public</url>
   </mirror>
   ```
5. **验证**（开新终端）：
   ```powershell
   mvn -version
   ```
   - 期望：输出 Maven 版本 + Java 版本

---

## Day 6：用 Maven 建第一个项目

1. 在 IDEA 里：New Project → 选 "Maven Archetype" 或直接 "Maven"
2. GroupId 填 `com.study`，ArtifactId 填 `hello-maven`
3. 建好后，在 `src/main/java` 下建 `HelloMaven.java`（内容同 Day 4）
4. 用 Maven 运行：在 IDEA 右侧 Maven 面板点 `hello-maven → Lifecycle → compile`
5. **验证**：`target/` 目录下生成 `.class` 文件 = 编译成功

---

## Day 7：复盘 + 把笔记提交到 GitHub

1. 在 `hello-java` 仓库里新建一个 `week1-notes.md`，记下：
   - 你装了哪些工具、什么版本
   - 踩了什么坑、怎么解决的
2. 提交推送：
   ```powershell
   git add .
   git commit -m "week1 environment setup notes"
   git push
   ```
3. 在 `学习路线.md` 里把"阶段 0"的勾打上 ✅

---

## 🧰 常见坑速查

| 问题 | 解决 |
|---|---|
| `java -version` 提示不是内部命令 | JDK 安装时没勾选 "Add to PATH"，或终端没重开。重装/重开终端 |
| `mvn -version` 找不到 | 环境变量 `MAVEN_HOME`/`Path` 没配对，检查拼写 |
| `git push` 要密码但总失败 | 2021 后 GitHub 不支持密码登录，改用 Personal Access Token 或 GitHub CLI |
| 下载东西特别慢 | Maven 配阿里云镜像；JDK/IDEA 用国内镜像站（如清华 TUNA） |
| 中文乱码 | IDEA → Settings → Editor → File Encodings，全设为 UTF-8 |

---

## ✅ 第一周结束自测

- [ ] `java -version`、`javac -version`、`git --version`、`mvn -version` 四条命令都能输出版本
- [ ] GitHub 上有一个 `hello-java` 仓库，里面有 README 和你的 week1 笔记
- [ ] IDEA 能新建 Java 项目并运行 Hello World
- [ ] 能用 Maven 编译一个项目

全部打勾 = 阶段 0 完成，可以进入阶段 1（Java 语法）了。
