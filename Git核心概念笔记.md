# Git 核心概念笔记：add / commit / push 详解

## 一、Git 的"四个地方"

数据在四个区域之间流动，每一步都需你主动触发，不会自动跳级。

```
①工作区 ──git add──▶ ②暂存区 ──git commit──▶ ③本地仓库 ──git push──▶ ④GitHub远程
```

| 区域 | 是什么 | 比喻 |
|---|---|---|
| ① 工作区 | 你直接能看/改的文件 | 桌上改好的文件 |
| ② 暂存区 | add 进来的待存档改动 | 打包台（挑好要寄的） |
| ③ 本地仓库 | commit 生成的正式存档 | 封好箱、贴标签的快递 |
| ④ 远程仓库 | GitHub 上的仓库 | 快递公司云端仓库 |

**关键**：改动不会自动跳级。不 add 到不了暂存区；不 commit 成不了存档；不 push 到不了 GitHub。

---

## 二、三大命令

### `git add .`
- 把工作区所有改动放进暂存区（打包台）
- `.` = 当前目录所有改动；也可 `git add 某文件` 只挑一个
- 此时还没真正存档，只是"标记为待存档"

### `git commit -m "说明"`
- 把暂存区的东西打包成一次正式存档，写进本地仓库
- `-m` = message，后面引号写这次存档干了啥
- commit 后：本地有存档了，但 GitHub 上还没变

### `git push`
- 把本地仓库的存档上传到 GitHub 远程
- push = "推"
- 执行后 GitHub 网页上才能看到这次改动

---

## 三、为什么 add 和 commit 要分两步？

为了精确控制每次存档放什么。举例（一次改了多个文件，想分两次清晰存档）：

```powershell
git add LoginService.java
git commit -m "修复登录bug"

git add RegisterService.java
git commit -m "新增注册功能"

git push
```

暂存区让你"挑挑拣拣"——一次只把相关改动封进一个箱，存档说明就清晰。

---

## 四、记忆口诀

**add 收集 → commit 封箱 → push 寄出**

---

## 五、配套常用命令

| 命令 | 作用 |
|---|---|
| `git status` | 看现在改动在哪个区（最常敲） |
| `git log --oneline` | 看本地所有存档历史 |
| `git diff` | 看工作区相比暂存区改了啥（还没 add 的改动） |
| `git diff --staged` | 看暂存区相比上次存档改了啥（add 了但没 commit 的） |
| `git clone 地址` | 把远程仓库整个下载到本地 |

---

## 六、总图

```
        改文件
   ┌──────────────┐
   │   ① 工作区    │  你改的东西
   └──────┬───────┘
        │ git add .
        ▼
   ┌──────────────┐
   │   ② 暂存区    │  挑好要存档的（打包台）
   └──────┬───────┘
        │ git commit -m "说明"
        ▼
   ┌──────────────┐
   │  ③ 本地仓库   │  正式存档（封好箱）
   └──────┬───────┘
        │ git push
        ▼
   ┌──────────────┐
   │ ④ GitHub远程  │  云端备份 + 给别人看
   └──────────────┘
```
